# import attr
# from apitist import converter
#
#
# class Name(str):
#     pass
#
#
# class LastName(str):
#     pass
#
#
# # converter.register_unstructure_hook(Name, lambda i, _: i)
# # converter.register_structure_hook(Name, lambda i, _: i)
# # converter.register_unstructure_hook(LastName, lambda i, _: i)
# # converter.register_structure_hook(LastName, lambda i, _: i)
